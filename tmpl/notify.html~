{% extends "base_main.html" %}

{% load my_formats %}

{% block content %}
  <div class="container-fluid">
      <div class="row">
        <div class="col-md-9 main">
                 {% include "side_menu_finance.html" %}
                <div class="col-md-9">
                    <h1 class="page-header">Уведомления</h1>
                </div>
                <div class="col-md-9 border_radius" id="transactions_block" >
                                              
                               <table class="table table-hover"  id='msgs'>                               
                                <thead>
                                                <tr>
                                                        <th> # </th>
                                                        <th> Дата </th>
                                                        <th> Сообщение </th>
                                                        <th> Действие </th>
                                                        
                                                </tr>
                                </thead>
                                 {% for item in msgs %}
                                        <tr id="notify_{{item.id}}" {% if item.user_seen_to in "false" %}  class="info"  "hip" {% endif %} >
                                                <td>{{item.id}}</td>
                                                <td>
                                                        <small>{{item.pub_date}}</small>
                                                </td>
                                                <td>
                                                     {{item.text}}
                                                     
                                                </td>
                                                <td>
                                                    <span onclick="Main.notify_remove({{item.id}})" class="btn btn-primary btn-xs">Hide</span></td>      
                                                </td>
                                        </tr>
                                {% endfor %}
                                
                                
                                
                                
                                </table>
                </div>          
       </div>
       {% include "scripts.html"%}


    </div>
  </div> 
{%endblock%}
